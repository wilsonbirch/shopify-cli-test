/// <reference types="node" resolution-mode="require"/>
/// <reference types="node" resolution-mode="require"/>
import '@shopify/shopify-api/adapters/node';
import { Server } from 'http';
import { Writable } from 'stream';
interface SetupGraphiQLServerOptions {
    stdout: Writable;
    port: number;
    appName: string;
    appUrl: string;
    apiKey: string;
    apiSecret: string;
    url: string;
    storeFqdn: string;
    scopes: string[];
    shopCustomDomain?: string;
}
export declare function setupGraphiQLServer({ stdout, port, appName, appUrl, apiKey, apiSecret, url, storeFqdn, scopes, shopCustomDomain, }: SetupGraphiQLServerOptions): Server;
export {};
